# Configure these in the process environment or your local secrets manager.
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=
GCS_BUCKET_NAME=
# Service-account JSON supplied securely to the server, never committed.
GOOGLE_APPLICATION_CREDENTIALS_JSON=
# Used by the schema tooling if provisioning a database.
DATABASE_URL=postgresql://localhost/workflow_capture
